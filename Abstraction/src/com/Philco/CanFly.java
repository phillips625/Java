package com.Philco;

/**
 * Created by PhillipsDaramola on 04/10/2017.
 */

// A good nameing convention for an interface is to name it what it is - Don't use 'I' in front of your interfaces.
    // The bat (which is a mammal) and all birds (via the Bird class) can now inherit from this interface.
public interface CanFly {
    void fly();
}
